 .. _users:

Users
======================================================================

A user can be part of multiple organizations. Only organizations can book a resource.
A user can have different roles in an organization: admin, booker.

An admin can add other users (by email) and promote them to admin or demote admin to bookers.

.. automodule:: re_sharing
   :members:
   :noindex:

